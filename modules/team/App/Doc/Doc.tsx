import { AppDocLayoutProps, AppDocLayout } from './AppDocLayout';

export const AppDoc = (props: AppDocLayoutProps) => {
  return (
    <AppDocLayout {...props}>
      <div>应用首页： TODO:</div>
    </AppDocLayout>
  );
};
